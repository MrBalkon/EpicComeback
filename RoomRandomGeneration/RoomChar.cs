using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoomChar : MonoBehaviour
{
    // Start is called before the first frame update
    public List<Vector3> doorsPos; 

    void Start()
    {
       
    }

   
    // Update is called once per frame
    void Update()
    {
        
    }
}
